/**
 * Created by Jams on 01/11/2017.
 */

// $('.qty+id, .price+id').on('input',function() {
//     var qty = parseInt($('.qty+id').val());
//     var price = parseFloat($('.price+id').val());
//     var retour = $('.total+id').val((qty * price ? qty * price : 0).toFixed(2));
// });

// $('.qty'+id).on('input',function() {
//     var qty = parseInt($('.qty'+id).val());
//     var price = parseFloat($('.price'+id).val());
//     var retour = $('.total'+id).val((qty * price ? qty * price : 0).toFixed(2));
// });
//
// $('.price'+id).on('input',function() {
//     var qty = parseInt($('.qty'+id).val());
//     var price = parseFloat($('.price'+id).val());
//     var retour = $('.total'+id).val((qty * price ? qty * price : 0).toFixed(2));
// });


    // $('.qty, .price').on('input',function() {
    // var qty = parseInt($('.qty').val());
    // var price = parseFloat($('.price').val());
    // var retour = $('.total').val((qty * price ? qty * price : 0).toFixed(2));
    // });



// $( document ).ready(function() {
//
//
//     $( ".pull" ).each(function() {
//
//
//         $('.qty'+id).on('input',function() {
//             var qty = parseInt($('.qty'+id).val());
//             var price = parseFloat($('.price'+id).val());
//             var retour = $('.total'+id).val((qty * price ? qty * price : 0).toFixed(2));
//         });
//
//         $('.price'+id).on('input',function() {
//             var qty = parseInt($('.qty'+id).val());
//             var price = parseFloat($('.price'+id).val());
//             var retour = $('.total'+id).val((qty * price ? qty * price : 0).toFixed(2));
//         });
//
//
//     });
//
// });






// $( "tr" )
//     .find( ".qty" ).val();
